# 🔍 Тест подключения к Backend

## Проблема: "Failed to fetch"

Это означает, что фронтенд не может подключиться к backend API.

---

## ✅ ШАГ 1: Проверьте что Backend запущен

### В терминале backend:
```bash
cd backend
node server.js
```

**Ожидаемый вывод:**
```
🚀 Portfolio Backend Server
================================
✅ Server running on port 3000
✅ Environment: development
✅ Uploads directory: /path/to/uploads

📡 Available endpoints:
   GET  /health
   ...
```

Если видите ошибку **PostgreSQL**:
```bash
# Создайте базу данных:
sudo -u postgres psql
CREATE DATABASE portfolio;
\q

# Проверьте .env файл:
nano backend/.env
```

---

## ✅ ШАГ 2: Проверьте что Frontend видит правильный URL

### Откройте браузер:
1. Перейдите на http://localhost:5173
2. Откройте **DevTools** (F12)
3. Посмотрите в **Console**

**Должны увидеть:**
```
🔄 Загрузка данных с API: http://localhost:3000/api/projects
✅ Projects loaded: {...}
✅ Profile loaded: {...}
```

**Если видите ошибку:**
```
❌ Error loading data: TypeError: Failed to fetch
📍 Убедитесь что backend запущен на: http://localhost:3000/api/projects
```

→ Backend НЕ запущен! Вернитесь к Шагу 1.

---

## ✅ ШАГ 3: Тест API напрямую

### В браузере откройте:
http://localhost:3000/health

**Должны увидеть:**
```json
{"status":"ok","timestamp":"2026-02-22T..."}
```

### Проверьте проекты:
http://localhost:3000/api/projects

**Должны увидеть:**
```json
{"success":true,"data":[]}
```

---

## ✅ ШАГ 4: Инициализируйте демо-данные

### Вариант A: Через curl
```bash
curl -X POST http://localhost:3000/api/init
```

### Вариант B: Через админку
1. Откройте http://localhost:5173/admin
2. Нажмите "Загрузить демо-данные"

### Вариант C: Через браузер
Откройте в новой вкладке:
```
http://localhost:3000/api/init
```
(Используйте POST расширение браузера)

---

## ✅ ШАГ 5: Проверьте .env файл

### Корень проекта (/.env):
```env
VITE_API_URL=http://localhost:3000
```

### Backend (/backend/.env):
```env
DB_HOST=localhost
DB_PORT=5432
DB_NAME=portfolio
DB_USER=postgres
DB_PASSWORD=your_password

PORT=3000
FRONTEND_URL=http://localhost:5173
```

---

## 🆘 Если все еще не работает

### 1. Перезапустите все:

**Терминал 1 (Backend):**
```bash
cd backend
node server.js
```

**Терминал 2 (Frontend):**
```bash
npm run dev
```

### 2. Проверьте порты:

```bash
# Проверьте что порт 3000 свободен:
lsof -i :3000

# Если занят - убейте процесс:
kill -9 [PID]
```

### 3. Проверьте PostgreSQL:

```bash
# Статус:
sudo systemctl status postgresql

# Перезапуск:
sudo systemctl restart postgresql
```

---

## ✅ Быстрый чеклист

- [ ] Backend запущен (видите логи в терминале)
- [ ] Видите "✅ Server running on port 3000"
- [ ] Порт 3000 не занят другим процессом
- [ ] PostgreSQL запущен и база создана
- [ ] Файл /.env существует с VITE_API_URL=http://localhost:3000
- [ ] Frontend запущен на http://localhost:5173
- [ ] В DevTools видите запросы к http://localhost:3000

---

## 🎉 Когда все работает

В DevTools Console должны видеть:
```
🔄 Загрузка данных с API: http://localhost:3000/api/projects
✅ Projects loaded: {success: true, data: [...]}
✅ Profile loaded: {success: true, data: {...}}
```

И на странице появятся проекты!
