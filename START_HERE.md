# 🚀 НАЧНИТЕ ОТСЮДА

## ⚡ БЫСТРЫЙ СТАРТ (без PostgreSQL)

### 1️⃣ Запустите Backend

**Откройте терминал:**
```bash
cd backend
npm install
npm run simple
```

**Ожидаемый вывод:**
```
🚀 Portfolio Backend Server (Simple Mode)
==========================================
✅ Server running on port 3000
✅ Using IN-MEMORY storage (no PostgreSQL needed)
✅ Demo data pre-loaded: 4 projects
```

⚠️ **НЕ ЗАКРЫВАЙТЕ этот терминал!**

---

### 2️⃣ Запустите Frontend

**Откройте ВТОРОЙ терминал:**
```bash
npm run dev
```

Откройте: http://localhost:5173

---

## 🎉 ГОТОВО!

Вы должны увидеть:
- ✅ 4 проекта на главной странице
- ✅ Работающую админку на /admin
- ✅ Загрузку файлов

---

## 🔄 Версия с PostgreSQL (опционально)

Если нужно постоянное хранение данных:

### 1. Установите PostgreSQL

**Ubuntu/Debian:**
```bash
sudo apt install postgresql
```

**macOS:**
```bash
brew install postgresql@14
brew services start postgresql@14
```

---

### 2. Создайте базу данных

```bash
sudo -u postgres psql
```

В консоли PostgreSQL:
```sql
CREATE DATABASE portfolio;
\q
```

---

### 3. Запустите с PostgreSQL

**Вместо `npm run simple` используйте:**
```bash
cd backend
npm start
```

---

## ✅ Проверка что все работает

### Откройте DevTools (F12) → Console

Должны видеть:
```
🔄 Загрузка данных с API: http://localhost:3000/api/projects
✅ Projects loaded: {success: true, data: [...]}
✅ Profile loaded: {success: true, data: {...}}
```

---

## 🎯 Разница между режимами

| Режим | Команда | PostgreSQL | Сохранение данных |
|-------|---------|------------|-------------------|
| **Simple** (рекомендуется для начала) | `npm run simple` | ❌ Не нужен | До перезапуска |
| **Full** (для production) | `npm start` | ✅ Требуется | Постоянно |

---

## 🆘 Если ошибка "Failed to fetch"

**Проверьте что Backend запущен:**
```bash
curl http://localhost:3000/health
```

Должно вернуть: `{"status":"ok",...}`

**Если нет** → см. [QUICKFIX.md](./QUICKFIX.md)

---

## 📚 Дополнительно

- **Быстрое исправление:** [QUICKFIX.md](./QUICKFIX.md) ⚡
- **Деплой на VPS:** [DEPLOYMENT.md](./DEPLOYMENT.md)
- **Полная документация:** [README.md](./README.md)