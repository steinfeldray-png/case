# 📝 ШПАРГАЛКА

## 🚀 ЗАПУСК (копируйте команды)

```bash
# === ТЕРМИНАЛ 1: Backend ===
cd backend
npm install              # Только первый раз
npm run simple          # Каждый раз

# === ТЕРМИНАЛ 2: Frontend ===
npm run dev             # Каждый раз
```

**Готово!** → http://localhost:5173

---

## 🔍 БЫСТРАЯ ДИАГНОСТИКА

### Проверка 1: Backend работает?
```bash
curl http://localhost:3000/health
```
✅ Должно вернуть: `{"status":"ok",...}`

### Проверка 2: API работает?
```bash
curl http://localhost:3000/api/projects
```
✅ Должно вернуть: `{"success":true,"data":[...]}`

### Проверка 3: Frontend видит backend?
Откройте DevTools (F12) → Console  
✅ Должны видеть: `✅ Projects loaded`

---

## 🛑 ИСПРАВЛЕНИЕ ОШИБОК

### "Failed to fetch"
```bash
# Проверьте Backend
curl http://localhost:3000/health

# Если не отвечает - запустите:
cd backend
npm run simple
```

### "Port 3000 already in use"
```bash
# Найдите процесс
lsof -i :3000

# Убейте процесс
kill -9 [PID]

# Запустите заново
cd backend
npm run simple
```

### "Cannot connect to PostgreSQL"
```bash
# Используйте простой режим вместо полного:
npm run simple   # ✅ Вместо npm start
```

### Пустая страница / Нет проектов
```bash
# Перезапустите Backend в простом режиме
cd backend
npm run simple
```

---

## 📦 УСТАНОВКА

```bash
# Установка всех зависимостей
npm install
cd backend && npm install && cd ..
```

---

## 🌐 АДРЕСА

| Что | URL |
|-----|-----|
| Главная | http://localhost:5173 |
| Админка | http://localhost:5173/admin |
| API | http://localhost:3000 |
| Health | http://localhost:3000/health |

---

## 📂 ФАЙЛЫ

| Файл | Для чего |
|------|----------|
| `/.env` | API URL для фронтенда |
| `/backend/.env` | Настройки backend |
| `/backend/server-simple.js` | Backend без БД |
| `/backend/server.js` | Backend с PostgreSQL |

---

## 🎯 КОМАНДЫ

### Frontend
```bash
npm run dev          # Запуск
npm run build        # Сборка
```

### Backend
```bash
cd backend
npm run simple       # БЕЗ PostgreSQL ⭐
npm start            # С PostgreSQL
```

---

## 🔄 РЕЖИМЫ

| Режим | Команда | PostgreSQL | Сохранение |
|-------|---------|------------|------------|
| Simple | `npm run simple` | ❌ | Временно |
| Full | `npm start` | ✅ | Постоянно |

**Для разработки:** используйте Simple  
**Для production:** используйте Full

---

## 💾 ДАННЫЕ

### Загрузить демо-данные
```bash
# Вариант 1: curl
curl -X POST http://localhost:3000/api/init

# Вариант 2: админка
http://localhost:5173/admin → "Загрузить демо-данные"
```

### Simple Mode
- Данные загружены по умолчанию
- Сохраняются пока сервер работает
- После перезапуска - сбрасываются

### Full Mode
- Данные в PostgreSQL
- Сохраняются навсегда
- Требует настройку БД

---

## 🆘 ЧТО ЧИТАТЬ

| Проблема | Документ |
|----------|----------|
| Не знаю с чего начать | [START_HERE.md](./START_HERE.md) |
| Backend не работает | [QUICKFIX.md](./QUICKFIX.md) |
| Failed to fetch | [FIX_ERROR.md](./FIX_ERROR.md) |
| Подробная установка | [INSTALL.md](./INSTALL.md) |
| Деплой на сервер | [DEPLOYMENT.md](./DEPLOYMENT.md) |

---

## ⚡ САМОЕ ВАЖНОЕ

```bash
# Запуск (2 терминала):

# Терминал 1
cd backend && npm run simple

# Терминал 2
npm run dev

# Готово!
# → http://localhost:5173
```

**Запомните:** `npm run simple` - это всё что нужно для старта! 🚀
