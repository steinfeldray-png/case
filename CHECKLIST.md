# ✅ ЧЕКЛИСТ ЗАПУСКА

Проверьте каждый пункт по порядку:

---

## 📦 Установка

- [ ] PostgreSQL установлен
- [ ] Node.js установлен (версия 18+)
- [ ] Выполнено `npm install` в корне проекта
- [ ] Выполнено `npm install` в папке `/backend`

---

## 🗄️ База данных

- [ ] PostgreSQL запущен (`sudo systemctl status postgresql`)
- [ ] База `portfolio` создана
- [ ] Пароль в `/backend/.env` совпадает с PostgreSQL

**Проверка:**
```bash
sudo -u postgres psql -l | grep portfolio
```

---

## ⚙️ Конфигурация

- [ ] Файл `/.env` существует
- [ ] В `/.env` указано `VITE_API_URL=http://localhost:3000`
- [ ] Файл `/backend/.env` существует
- [ ] В `/backend/.env` указаны правильные данные БД

**Проверка:**
```bash
cat .env
cat backend/.env
```

---

## 🚀 Запуск

- [ ] Backend запущен в Terminal 1
- [ ] Вижу "✅ Server running on port 3000"
- [ ] Вижу "✅ Connected to PostgreSQL database"
- [ ] Frontend запущен в Terminal 2
- [ ] Вижу "Local: http://localhost:5173"

---

## 🧪 Тестирование

- [ ] http://localhost:3000/health возвращает JSON
- [ ] http://localhost:3000/api/projects возвращает `{"success":true,...}`
- [ ] http://localhost:5173 открывается
- [ ] В DevTools нет красных ошибок
- [ ] Вижу лог "🔄 Загрузка данных с API"
- [ ] Вижу лог "✅ Projects loaded"

---

## 📊 Данные

- [ ] Выполнено `/api/init` (демо-данные загружены)
- [ ] На главной странице видны проекты
- [ ] Админка открывается (http://localhost:5173/admin)

---

## ❌ Если что-то не работает

### Backend не запускается:
```bash
# Проверьте порт
lsof -i :3000
# Если занят - освободите
kill -9 [PID]
```

### PostgreSQL не работает:
```bash
# Ubuntu/Debian
sudo systemctl restart postgresql

# macOS
brew services restart postgresql@14
```

### База данных не создается:
```bash
# Попробуйте от другого пользователя
psql -U postgres
CREATE DATABASE portfolio;
```

### Frontend не видит Backend:
```bash
# Проверьте .env
cat .env
# Должно быть: VITE_API_URL=http://localhost:3000

# Перезапустите frontend
# Ctrl+C в Terminal 2
npm run dev
```

---

## 🎯 Итоговая проверка

Открыть в браузере:

1. ✅ http://localhost:3000/health → `{"status":"ok"}`
2. ✅ http://localhost:3000/api/projects → `{"success":true,"data":[...]}`
3. ✅ http://localhost:5173 → Видны проекты
4. ✅ http://localhost:5173/admin → Админка работает

**Все 4 пункта ✅?** → **Готово!** 🎉

---

## 📞 Помощь

Если что-то не работает:
1. См. [FIX_ERROR.md](./FIX_ERROR.md)
2. См. [TEST_CONNECTION.md](./TEST_CONNECTION.md)
3. Проверьте логи в Terminal 1 и Terminal 2
4. Проверьте DevTools → Console (F12)
