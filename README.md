# 🎨 Portfolio - Alexander Petrov

Современное портфолио продуктового дизайнера с админ-панелью для управления контентом.

## 📋 Описание

Портфолио включает:
- **Главную страницу** с информацией о дизайнере и проектах
- **Страницу кейса** с детальным описанием проекта
- **Админ-панель** для управления проектами и профилем
- **Загрузку файлов** (изображения, CV)
- **Адаптивный дизайн** с SF Pro шрифтом

## 🏗️ Архитектура

Проект использует **Вариант A: Vercel + VPS**

### Frontend
- **Платформа:** Vercel
- **Технологии:** React 18 + TypeScript + Tailwind CSS v4
- **Роутинг:** React Router v7
- **Стоимость:** Бесплатно

### Backend
- **Платформа:** Российский VPS (Timeweb/Beget)
- **Технологии:** Node.js + Express + PostgreSQL
- **Storage:** Локальное хранилище
- **Стоимость:** ~250₽/мес

## 🚀 Быстрый старт

### Вариант A: Без PostgreSQL (рекомендуется для начала)

1. **Запустите backend:**
```bash
cd backend
npm install
npm run simple
```

2. **Запустите frontend (в новом терминале):**
```bash
npm run dev
```

Приложение откроется на `http://localhost:5173`

### Вариант B: С PostgreSQL (для production)

См. подробные инструкции в файле [START_HERE.md](./START_HERE.md)

## 📦 Развертывание

См. подробные инструкции в файле [DEPLOYMENT.md](./DEPLOYMENT.md)

### Кратко:

**Frontend → Vercel:**
```bash
# Подключите репозиторий к Vercel
# Добавьте переменную окружения:
# VITE_API_URL=http://ВАШ_IP_АДРЕС
```

**Backend → VPS:**
```bash
# На сервере:
cd /var/www/portfolio/backend
npm install
npm start  # или используйте PM2
```

## 🛠️ Технологии

### Frontend
- React 18.3.1
- TypeScript
- React Router v7
- Tailwind CSS v4
- Lucide React (иконки)
- Vite

### Backend
- Node.js 18+
- Express.js
- PostgreSQL
- Multer (загрузка файлов)
- CORS

## 📁 Структура проекта

```
/
├── backend/                 # Backend API
│   ├── server.js           # Express сервер
│   ├── db.js               # PostgreSQL логика
│   ├── package.json
│   └── .env.example
│
├── src/
│   ├── app/
│   │   ├── App.tsx         # Главный компонент
│   │   ├── pages/          # Страницы
│   │   │   ├── HomePage.tsx
│   │   │   ├── CaseStudy.tsx
│   │   │   └── Admin.tsx
│   │   └── data/
│   │       └── projects.ts # Типы данных
│   │
│   ├── config/
│   │   └── api.ts          # API конфигурация
│   │
│   └── styles/             # Стили
│       ├── fonts.css
│       └── theme.css
│
├── DEPLOYMENT.md           # Инструкции по деплою
├── vercel.json             # Конфиг Vercel
└── package.json
```

## 🔑 API Endpoints

### Projects
- `GET /api/projects` - Все проекты
- `GET /api/projects/:slug` - Проект по slug
- `POST /api/projects` - Создать проект
- `PUT /api/projects/:id` - Обновить проект
- `DELETE /api/projects/:id` - Удалить проект

### Profile
- `GET /api/profile` - Получить профиль
- `PUT /api/profile` - Обновить профиль

### Upload
- `POST /api/upload` - Загрузить файл

### Utility
- `GET /health` - Health check
- `POST /api/init` - Загрузить демо-данные

## 🌍 Доступность из России

✅ **Работает без VPN** благодаря:
- Frontend на Vercel (доступен из РФ)
- Backend на российском VPS
- Нет зависимости от Supabase

## 📄 Лицензия

MIT

## 👤 Автор

Alexander Petrov  
Telegram: [@saneuuu](https://t.me/saneuuu)