# 📦 УСТАНОВКА И ЗАПУСК

## ⚡ ДЛЯ НЕТЕРПЕЛИВЫХ

Скопируйте и вставьте в терминал:

```bash
# Терминал 1
cd backend && npm install && npm run simple

# Терминал 2 (откройте новый терминал)
npm run dev
```

Откройте: http://localhost:5173

**ГОТОВО!** 🎉

---

## 📝 ПОДРОБНАЯ ИНСТРУКЦИЯ

### Шаг 1: Установка зависимостей

```bash
# Установите зависимости фронтенда
npm install

# Установите зависимости backend
cd backend
npm install
cd ..
```

---

### Шаг 2: Запуск Backend

**Откройте Терминал 1:**

#### Вариант A: Простой режим (БЕЗ PostgreSQL)
```bash
cd backend
npm run simple
```

Вы увидите:
```
🚀 Portfolio Backend Server (Simple Mode)
==========================================
✅ Server running on port 3000
✅ Using IN-MEMORY storage (no PostgreSQL needed)
✅ Demo data pre-loaded: 4 projects
```

#### Вариант B: Полный режим (С PostgreSQL)
```bash
cd backend
npm start
```

Требует:
- Установленный PostgreSQL
- Созданную базу данных `portfolio`
- Настроенный `.env` файл

---

### Шаг 3: Запуск Frontend

**Откройте Терминал 2:**

```bash
npm run dev
```

Вы увидите:
```
VITE v6.3.5  ready in XXX ms

➜  Local:   http://localhost:5173/
➜  Network: use --host to expose
```

---

### Шаг 4: Откройте в браузере

**Главная страница:**  
http://localhost:5173

**Админ-панель:**  
http://localhost:5173/admin

**API Health:**  
http://localhost:3000/health

---

## ✅ ПРОВЕРКА

После запуска должны видеть:

### В Терминал 1 (Backend):
```
✅ Server running on port 3000
✅ Using IN-MEMORY storage
✅ Demo data pre-loaded: 4 projects
```

### В Терминал 2 (Frontend):
```
VITE v6.3.5  ready
➜  Local:   http://localhost:5173/
```

### В браузере:
- ✅ 4 проекта на главной странице
- ✅ Профиль Alexander Petrov
- ✅ Админка доступна

### В DevTools Console (F12):
```
🔄 Загрузка данных с API: http://localhost:3000/api/projects
✅ Projects loaded: {success: true, data: [...]}
✅ Profile loaded: {success: true, data: {...}}
```

---

## 🔄 РЕЖИМЫ ЗАПУСКА

### Simple Mode (рекомендуется для разработки)

**Команда:**
```bash
cd backend && npm run simple
```

**Особенности:**
- ✅ Не требует PostgreSQL
- ✅ Данные предзагружены
- ✅ Работает из коробки
- ⚠️ Данные не сохраняются после перезапуска

### Full Mode (для production)

**Команда:**
```bash
cd backend && npm start
```

**Особенности:**
- ✅ Постоянное хранение в PostgreSQL
- ✅ Данные сохраняются
- ⚠️ Требует настройки БД

---

## 🛑 ОСТАНОВКА

### Остановить Backend
В Терминал 1 нажмите: `Ctrl + C`

### Остановить Frontend
В Терминал 2 нажмите: `Ctrl + C`

---

## 🔄 ПЕРЕЗАПУСК

Просто повторите команды запуска:

```bash
# Терминал 1
cd backend && npm run simple

# Терминал 2
npm run dev
```

---

## 📊 ДОСТУПНЫЕ КОМАНДЫ

### Frontend
```bash
npm run dev          # Запуск dev сервера
npm run build        # Сборка для production
npm run preview      # Просмотр production сборки
```

### Backend
```bash
cd backend
npm run simple       # Запуск без PostgreSQL
npm run simple:dev   # Запуск с hot-reload
npm start            # Запуск с PostgreSQL
npm run dev          # Запуск с hot-reload и PostgreSQL
```

---

## 🌐 ПОРТЫ

| Сервис | URL | Порт |
|--------|-----|------|
| Frontend | http://localhost:5173 | 5173 |
| Backend API | http://localhost:3000 | 3000 |
| PostgreSQL | localhost | 5432 |

---

## 🎯 СЛЕДУЮЩИЕ ШАГИ

После успешного запуска:

1. **Попробуйте админку:**  
   http://localhost:5173/admin

2. **Добавьте свой проект:**  
   Админка → "Создать проект"

3. **Настройте профиль:**  
   Админка → Вкладка "Профиль"

4. **Загрузите изображения:**  
   Используйте кнопку загрузки в форме проекта

---

## 🆘 ПРОБЛЕМЫ?

### Backend не запускается
- Проверьте что порт 3000 свободен: `lsof -i :3000`
- Установите зависимости: `cd backend && npm install`

### Frontend не запускается  
- Проверьте что порт 5173 свободен: `lsof -i :5173`
- Установите зависимости: `npm install`

### Failed to fetch
- Backend не запущен - см. Шаг 2
- Проверьте `.env` файл в корне проекта
- Проверьте http://localhost:3000/health

### Нет проектов на странице
- Используйте `npm run simple` вместо `npm start`
- Проверьте DevTools Console на ошибки
- Перезапустите backend

---

## 📚 ДОПОЛНИТЕЛЬНАЯ ДОКУМЕНТАЦИЯ

- [START_HERE.md](./START_HERE.md) - Начало работы
- [QUICKFIX.md](./QUICKFIX.md) - Быстрое исправление ошибок
- [DEPLOYMENT.md](./DEPLOYMENT.md) - Деплой на сервер
- [README.md](./README.md) - Общая информация
